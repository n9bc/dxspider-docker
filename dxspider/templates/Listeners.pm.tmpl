# Listeners.pm — DXSpider telnet listener configuration
# Generated by entrypoint.sh from Listeners.pm.tmpl.
# This file is written on first run (or when OVERWRITE_CONFIG=yes).
# Operator edits survive container restarts because /spider/local is a
# named volume; the file is only replaced when absent or OVERWRITE_CONFIG=yes.
#
# Token substitution map (entrypoint.sh sed replacements):
#   __TELNET_PORT__  <- NODE_TELNET_PORT  (optional, default 7300)
#
# Format: each element is an arrayref [ host, port ] or
#         [ host, port, "option" ] — see DXSpider docs for full options.
#
# IMPORTANT: DXSpider iterates @main::listen (see cluster.pl: `foreach my $l
# (@main::listen)`).  The listener array MUST live in the `main::` package as
# a plain global — do NOT add `package Listeners;`, `use strict;`, or `our`,
# or the array ends up in @Listeners::listen, DXSpider sees no listeners, and
# the node ceases immediately after "starting listeners ...".

package main;

@listen = (
    ["0.0.0.0", __TELNET_PORT__],
);

1;   # Perl module — required true return value
